#!/usr/bin/env bash
# Placeholder wrapper to prevent CI scripts from failing when trying to call ./gradlew
# The actual Android wrapper is generated by `expo prebuild --platform android` under ./android/gradlew
echo "Gradle wrapper not initialized. Skipping."
exit 0
